version: "v1.0"
name: Pipelin
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Test"
    task:
      jobs:
      - name: Tests
        commands:
          - checkout
          - mix deps.get
          - mix test
